# 科学上网第一课：先搞懂运营商，再谈节点

> 一台 ping 45 ms 的机器，请求为什么会卡 1.27 秒。从这个问题往下讲：你家是哪张网、买之前五分钟看路、联通绕美怎么用 XHTTP 套 CDN 绕开、电信移动各自的毛病、买哪家、解锁 TikTok / AI 为什么要走两跳，还有上线前三个开关。
>
> 网页版：https://blog.songchengtie.me/posts/kexue-shangwang-01/ · 给 AI 的执行版：[AGENT.md](./AGENT.md)


先看一组我自己的数。深圳移动，Linode 新加坡，裸 ping 稳定在 45 ms 左右。同一个晚上，用 curl 通过代理请求 `https://www.gstatic.com/generate_204`，跑十次：


| 窗口 | 协议 | 成功 | P50 | P95 | 最大 |
| --- | --- | ---: | ---: | ---: | ---: |
| 第一批 | Hysteria2 · BBR | 10/10 | 267 ms | 1269 ms | 1269 ms |
| 同窗对照 | Reality · TCP 2053 | 10/10 | 335 ms | 383 ms | 383 ms |
| 第二批 | Hysteria2 · BBR | 10/10 | 255 ms | 281 ms | 281 ms |

ping 45 ms 的机器，一个空请求的中位数是 255 到 335 ms，最慢的一次 1.27 秒。ping 没变，慢了 28 倍。测速软件说这是一条好线，实际用起来像在拨号。那一秒多花在哪了？

<figure>
  <img src="https://blog.songchengtie.me/images/tail.webp" alt="上面是 ping，下面是一次完整请求，同样的两个端点" width="900" height="900" loading="lazy">
  <figcaption><span>Fig. 01 · 上面那条是 ping，下面那条是一次完整请求，两头是同样的两个点</span><span>Plate Nº 02</span></figcaption>
</figure>

ping 只量到 VPS 为止。一次真正的请求还要做 TLS 握手，经过代理转发，再从出口走到目标网站，每一段都可能在路上绕、在晚高峰堵。这篇要讲的就是那些段：它们由你家的运营商和对方机房决定，所以网上任何人的测速对你都没用，你得自己看路。每一节的标题就是一个问题，你带着哪个问题来，就跳到哪一节。看完你应该知道自己家是哪张网，买之前怎么花五分钟确认不绕路，联通用户怎么把 1800 ms 压到 400，以及要碰 TikTok 和 AI 服务为什么得走两跳。

## 01 为什么别人的测速对我没用？

一条线路快不快，两头一起决定：你家宽带从哪个国际出口出去，对方机房接的是哪家上游，两边在哪个交换点碰头。三家运营商是三套完全不同的出口体系：

| 运营商 | 普通线路 | 精品线路 | 特点 |
| --- | --- | --- | --- |
| 电信 | 163 骨干（AS4134） | CN2 GT / CN2 GIA（AS4809） | 163 晚高峰拥塞最严重，GIA 最贵 |
| 联通 | 169 骨干（AS4837） | AS9929（原网通 A 网） | 4837 国际出口在北上广，回程经常走美国 |
| 移动 | CMI（AS58453） | CMIN2 | 在香港 / 新加坡有直连交换点，华南到东南亚很短 |

<figure>
  <img src="https://blog.songchengtie.me/images/nets.webp" alt="同一片海岸，三条线各走各的" width="900" height="900" loading="lazy">
  <figcaption><span>Fig. 02 · 同一片海岸出去，三张网各走各的路，最远那条还得绕回来</span><span>Plate Nº 03</span></figcaption>
</figure>

同一台 VPS，三网测出来能差好几倍。测评站跑的是他们的机房、他们的运营商、他们那个时间段，跟你家没关系。你只能信自己在家、在常用的时间段、用自己的宽带测出来的数。

三张网里差别最大的是联通，它的回程有个结构性的问题，03 节单独讲。在那之前，先说怎么在花钱之前把路看清楚，这套方法三网通用。

## 02 买之前怎么知道会不会绕路？

不用买了再测。每家商家都有测试 IP（test IP）或者 looking glass，够你在下单前把去程和回程都看一遍。顺序是这样：

1. 找 test IP / looking glass。Linode 每个机房都有 `speedtest.<机房>.linode.com`（新加坡是 `speedtest.singapore.linode.com`）；Vultr、DMIT、搬瓦工在官网都列了各机房的测试 IP；不少商家的 looking glass 页面能直接跑 ping / traceroute / mtr。
2. 去程：本机 besttrace 到 test IP，看路径上经过的 AS。联通的 `219.158.x.x` 之后出现美国节点，就是绕美。
3. 回程：在 looking glass 上 traceroute 你家的公网 IP（先 `curl -4 ip.sb` 拿到）。回程比去程重要，绕路基本都绕在回程。商家没有 looking glass 的，找测评站的回程路由截图，延迟数字不能信，但路径经过哪些 AS 是对的。或者买台按小时计费的机器，测一小时退掉。
4. 晚高峰（21 点到 23 点）再跑一遍。白天 40 ms 晚上 200 ms 还丢包 10% 的线路很多。
5. 用 ping 反推。广东移动直连的参考值：香港 10 ms 出头，新加坡 40 到 50，东京 60 到 80，美西 130 到 160。一台香港机器 ping 出来 150 多，不用看 traceroute 也知道回程绕了美国。

```bash
# 去程：本机到商家的 test IP
./besttrace -q 1 speedtest.singapore.linode.com

# 拿到自己的公网 IP，贴到商家的 looking glass 里跑回程
curl -4 ip.sb
```

想再深一层，到 bgp.tools 查机房 ASN 的 upstream / peer。和 CMI（AS58453）、CTG（AS23764）、联通（AS4837 / AS10099）有直接 peering 的机房才可能直连；上游只有 NTT、Telia、Cogent、GTT 这类国际 Tier 1 的，联通十有八九绕美。

买了之后，把开头那四步在自己机器上再跑一遍：ping，mtr 或 besttrace 看路径（去程本机跑，回程在 VPS 上往你家 IP 跑），晚高峰复测，最后用 curl 走代理跑十次完整请求看 P50 和 P95。最后一步不能省，Table 01 那个 1.27 秒就是它跑出来的，ping 看不到长尾。

```bash
# 去程：本机到 VPS
mtr -rwc 50 <vps-ip>

# 回程：在 VPS 上装 besttrace，跑回你家的公网 IP
./besttrace -q 1 <你的公网ip>

# 完整代理请求，跑 10 次看长尾
for i in $(seq 10); do
  curl -x socks5h://127.0.0.1:7890 -o /dev/null -s \
    -w '%{time_total}\n' https://www.gstatic.com/generate_204
done
```

这套流程跑下来，联通用户十有八九会在第 3 步看到美国节点。下面说这是怎么回事，以及怎么办。

## 03 联通为什么特别慢？怎么绕开？

联通用户最常碰到的情况：去程看着正常，回程从 VPS 回国的时候，路由从新加坡或者香港先跑到美国西海岸（圣何塞、洛杉矶），再从北上广的 169 出口进来。太平洋来回一趟，延迟翻几倍，晚高峰还丢包。

<figure>
  <img src="https://blog.songchengtie.me/images/detour.webp" alt="两个点明明挨着，回程却绕了一整个太平洋" width="900" height="900" loading="lazy">
  <figcaption><span>Fig. 03 · 两个点明明挨着，回程却绕了一整个太平洋</span><span>Plate Nº 04</span></figcaption>
</figure>

原因在结构上。AS4837 和海外运营商的对等互联点大量落在美国，海外机房的上游如果没在亚洲跟联通直接互联，BGP 就会选那条 AS 路径最短的路。AS 路径短，物理距离长。这段由机房的上游决定，用户端改不了，换协议也没用。

判断方法：在 VPS 上 besttrace 回你家 IP，`219.158.x.x`（4837 骨干）之前出现了美国节点，就是绕了。

路由本身改不了，但可以绕开它。三条路，按我推荐的顺序。

第一条，套 CDN。这是我最近才试出来的，效果最好。思路是客户端不直连 VPS，先连 Cloudflare 的边缘节点，再由 Cloudflare 的骨干网把流量送到 VPS。传输层用 Xray 的 XHTTP（原来叫 SplitHTTP），它把上下行拆成普通的 HTTPS 请求和流式响应，在 Cloudflare 看来就是一个正常网站，所以能过 CDN。WebSocket 也能过，但 XHTTP 在 HTTP/2 和 HTTP/3 上表现更好。域名接入 Cloudflare，开代理（小云朵），DNS 解析出来的就是 Cloudflare 的 IP 而不是你的 VPS。然后做优选 IP：Cloudflare 的 anycast IP 段里，有一部分从国内过去会落在香港（HKG）的边缘节点，把这批 IP 填到客户端的 `server` 字段，SNI 和 Host 仍然写自己的域名，等于强制走香港。回程变成 VPS 到 Cloudflare 骨干、到香港边缘、再到联通。Cloudflare 和联通在香港有互联，不用再跨太平洋。

<figure>
  <img src="https://blog.songchengtie.me/images/cdn.webp" alt="上面虚线是直连的回程，下面实线是经过 CDN 香港节点的回程" width="900" height="900" loading="lazy">
  <figcaption><span>Fig. 04 · 同一台机器的两条回程：虚线直连绕美，1800 ms；实线过 CDN 香港节点，400 ms</span><span>Plate Nº 05</span></figcaption>
</figure>

我实测同一台绕美的机器，直连时完整请求 P50 在 1800 ms 上下，套 CDN 强制走香港之后降到 400 ms 左右，而且稳很多，CDN 骨干不像公网那样晚高峰乱跳。VPS 的真实 IP 也不再暴露了，被封的只会是 CDN 的 IP，换一个就行，Cloudflare 的 IP 段也不可能被整体封掉。代价是多了一层 HTTP，单次请求的底噪比直连高一点，而且免费版有公平使用限制，跑大流量不合适。

第二条，认了绕美，直接买美西，再靠 Hysteria2 暴力堆。联通到美西这一段反而是三家里优化得最好的。既然回程一定要过美西，那把机器就放在洛杉矶或者圣何塞，去程回程都只跨一次太平洋。然后用 Hysteria2 的 Brutal 拥塞控制硬顶：在配置里写上下行带宽，它就按这个速率固定发包，丢多少补多少，不像 TCP 一丢包就缩窗口。这样完整请求能压到 100 到 200 ms，晚高峰也稳。前提是你的宽带对 UDP 友好，这个 04 节再说。

第三条，AS9929 / CUII 精品线路。贵，也不是每个机房都有，但这是唯一一条路由本身就不绕的路。北方联通、晚高峰 169 堵得厉害、又不想折腾 CDN 的，可以考虑。

## 04 电信和移动各有什么毛病？

| 运营商 | 该买哪儿 | 别买哪儿 | 备注 |
| --- | --- | --- | --- |
| 移动 | 香港、新加坡、日本 | — | 沿海首选；但经常静默丢 443 的包，UDP 更甚 |
| 电信 | 日本（东京、大阪）、香港 | 美国普通线路 | 163 到日本、香港直连较好；开 Hysteria2 感知明显 |
| 联通 | 美西 + Hy2 Brutal；或任意机房 + XHTTP 套 CDN 走香港 | 香港、新加坡直连 | 见 03 节 |

电信的 163 到日本（东京、大阪）和香港的直连比到东南亚好。电信的主要问题是晚高峰拥塞，不是绕路。在电信上开 Hysteria2 能明显降延迟：QUIC 加 BBR 对丢包不敏感，163 晚高峰那种 5% 到 10% 的丢包，TCP 会被拖成秒级卡顿，Hy2 基本无感。我在移动上的对照也是同一个方向，Table 01 里 Hy2 的 P50 比 Reality 低 24%（255 vs 335 ms），P95 低 27%（281 vs 383 ms）。

移动在沿海仍然是第一选择，直连，延迟低。CMI 在香港 HKIX 和新加坡都有直连交换点，我在深圳移动测东南亚机房，一般都比同一台机器走联通好。但两个毛病要知道。一是经常把 443 端口的包静默丢掉，你看到的只是超时，没有任何拒绝信息。二是对 UDP 类协议（Hysteria2、TUIC、WireGuard）有 QoS，跑一会儿就限速甚至断流。对策是 Hy2 不用 443，换一个高位端口，或者开端口跳跃（port hopping），同时留一个 TCP 的 Reality 节点做备用。

再就是 Hysteria2 本身。它只在 UDP 友好的环境里才强。QUIC 加 BBR 或者 Brutal 抗的是丢包，抗不了运营商对 UDP 的限速和断流。电信、联通大部分地区 UDP 放得开，Hy2 是首选；移动那种跑一会儿就 QoS 的，或者公司、校园网直接掐 UDP 的，Hy2 反而不如老老实实的 TCP Reality 稳。先用 `iperf3 -u`，或者干脆跑一小时 Hy2 看会不会掉速，再决定主用哪个。

线路定了，剩下的问题是钱：便宜的机器能不能买，贵的精品线路值不值。

## 05 精品线路值不值？国内云为什么不能买？

先说精品线路。CN2 GIA、CMIN2、AS9929 的价格通常是普通线路的 3 到 10 倍，它们解决两个问题：绕路和晚高峰拥塞。如果你在广东这样的沿海地区，尤其用移动，这两个问题本来就不严重，不绕路的情况下到香港 10 ms 出头，到新加坡 40 到 50 ms，精品线路能帮你省的也就十来毫秒。花三倍价钱把 45 ms 变成 35 ms，感觉不出来。什么时候值得买：北方或者内陆的电信、联通用户，晚高峰 163 / 169 明显拥塞的；或者你已经确认自己的回程在绕美。先测，再决定。

普通线路两档。Linode 新加坡最低配 5 刀一个月（36 元），够一两个人日常用，IP 干净，商家稳。ClawCloud 有 70 块一年的促销，价格只有 Linode 的十分之一。它要分区看：新加坡区是个特例，机器直接挂在联通的出口上，联通用户走它反而顺，圈子里叫联通快乐机，但这不改变联通整体最好是美西的结论；电信和移动优先看它的日本、香港区，也不算差。缺点是 IP 比较脏，被大量用户用过、被各家标记过，日常看 YouTube、查资料没影响，拿它做出口去碰 06 节那种看 IP 纯净度的服务就基本没戏。日常一台 ClawCloud，解锁走两跳，是个便宜的组合。商家更多的比较在第二课。

再便宜也别碰的是国内云。腾讯云、阿里云的香港轻量服务器一个月二三十块，但有三个原因不能买。第一，必须实名，实例和你的身份证绑在一起，出问题不是封个 IP 那么简单。第二，协议明文禁止。阿里云轻量应用服务器的规则写明"禁止做流量穿透服务，违规者最高关停并锁定实例、清退处理"，阿里云云防火墙还有专门的"禁止代理行为"IPS 规则文档。第三，会主动检测。V2EX 上有人在阿里云国际香港轻量新开的机器上装 SS，端口立刻被阻断；腾讯云 2022 年就批量封过跑代理的轻量实例，2026 年又有一轮针对代理行为的严查。海外商家（Linode、Vultr、DMIT 之类）不实名，不检测你的流量，封了也就是换台机器。

## 06 TikTok 和 ChatGPT 为什么要走两跳？

看 YouTube、查 Google，随便一个梯子都行，出口 IP 是哪的无所谓。

TikTok、ChatGPT 这类服务看的是出口 IP 的纯净度。它们会查 IP 的类型（机房还是住宅）、ASN、有没有被标记成代理。机房 IP 的下场是 TikTok 零播放，Claude 提示 unusual activity 或者直接拒绝注册。这和商家好坏无关，搬瓦工、DMIT 的机房 IP 一样中招，因为 ASN 上就写着 hosting。严格程度分档：ChatGPT、Gemini 不算严，出站套一层 WARP 就能过；Claude 最严，只认住宅 IP，也就是 ISP 静态住宅，美国住宅 IP 的解锁能力最强。第二课 05 节展开。

问题是住宅 IP 的 SOCKS5 代理一般是裸协议，没有伪装，从国内直连一定会被 GFW 识别并阻断。GFW 对代理流量的识别早就不是看端口了。2023 年 USENIX Security 的论文证实它能实时识别全加密流量，社区也在 2022 到 2023 年反复观测到 TLS-in-TLS 特征被精准识别。

所以要走两跳：

```
本机 ──(Reality / Hysteria2，抗识别)──▶ 香港 VPS ──(SOCKS5)──▶ 美国住宅 IP ──▶ TikTok / ChatGPT
        第一跳：离你最近，负责跳出去          第二跳：国际互联网，几乎不慢          目标只看到住宅 IP
```

<figure>
  <img src="https://blog.songchengtie.me/images/hops.webp" alt="第一块石头要近，墙上的缝要小" width="900" height="729" loading="lazy">
  <figcaption><span>Fig. 05 · 第一跳要近（灰到珊瑚），墙在第一跳；过了墙就是公海，第二跳再远也不慢</span><span>Plate Nº 06</span></figcaption>
</figure>

第一跳只做一件事：用最短的物理距离、最难被识别的协议跳出去。在华南，香港是最优解，10 ms 出头。跳出去之后剩下的路是国际互联网，香港到美国西海岸 150 ms 左右，这一段一般不慢，也没有 GFW。

两跳在 Clash / Mihomo 里就是 `dialer-proxy`：SOCKS5 出口节点的 `dialer-proxy` 指向香港节点。

## 07 上线前还要开哪三个开关？

线路和协议都定了，还有三个开关不开，前面的功夫会漏掉一半。

把 IPv6 封掉。家宽的 IPv6 前缀是动态的，经常变（漂移），你的白名单、防火墙规则、VPS 的 IPv6 都会跟着失效。更麻烦的是系统解析到 AAAA 记录时会优先走 IPv6，这部分流量可能绕开代理直接出去。做法：网卡上直接禁用 IPv6，Clash 配置里 `ipv6: false`，VPS 上也关掉。

WebRTC 泄露。浏览器的 WebRTC 会通过 STUN 拿到你的真实公网 IP，绕过代理直接暴露。装 WebRTC Control 这类扩展，或者在 Chrome 策略里把 WebRTC IP handling 设成 disable non-proxied UDP。到 browserleaks.com/webrtc 检查。

Clash 开 fake-ip。`redir-host` 模式会先做真实 DNS 解析再匹配规则，解析请求经常走本地运营商的 DNS，一是被污染，二是运营商能看到你在解析什么，这就是 DNS 泄露。`fake-ip` 模式不做本地解析，直接返回 `198.18.0.0/15` 段的假 IP，把域名交给远端解析。局域网设备、游戏平台之类需要真实 IP 的加到 `fake-ip-filter` 里。到 ipleak.net 检查，DNS 结果里不该出现你运营商的服务器。

```yaml
ipv6: false
dns:
  enable: true
  enhanced-mode: fake-ip
  fake-ip-range: 198.18.0.1/16
  fake-ip-filter:
    - '*.lan'
    - '+.local'
    - 'localhost.ptlogin2.qq.com'
    - '+.stun.*.*'
    - '+.msftconnecttest.com'
  nameserver:
    - https://dns.alidns.com/dns-query
  fallback:
    - https://1.1.1.1/dns-query
```

## 08 下一课

路会看了，接下来三课把机器买回来、装上、连上：第二课 机器从哪来（免费的三家、便宜商家、查 IP 有没有被墙），第三课 连上去装起来（SSH、协议、3x-ui、脚本、让 AI 装），第四课 客户端和 DNS。03 节那个 1800 到 400 的方案，服务端配置在第三课的 03 节。给 AI 的执行版四课合成一份，就是开头那个框。

## 参考

- 中国联通骨干网介绍以及线路区别：AS4837 / AS9929 / AS10099 — https://zhuanlan.zhihu.com/p/528764437
- VPS 三网直连线路怎么选：CN2 GIA、CMIN2、9929、CMI、4837 对比 — https://vpsdaohang.com/185.html
- 搬瓦工香港 HK85 机房电信/联通取消 CMI 直连改走 NTT 绕路 — https://www.banwagongcn.com/2084.html
- Wu et al., "How the Great Firewall of China Detects and Blocks Fully Encrypted Traffic", USENIX Security 2023 — https://www.usenix.org/conference/usenixsecurity23/presentation/wu-mingshi
- v2fly 讨论：tls+ws 是否被 GFW 精准识别 — https://github.com/v2fly/v2ray-core/discussions/2036
- 阿里云：配置云防火墙 IPS 禁止代理行为 — https://help.aliyun.com/zh/cloud-firewall/cloudfirewall/use-cases/disable-proxies
- V2EX：阿里云国际 HK 轻量新开服务上 SS 立马被阻断端口 — https://www.v2ex.com/t/850663
- 腾讯云封锁使用翻墙的轻量服务器（2022） — https://elmagnifico.tech/2022/07/25/TXCloud-Ban/
- Clash DNS 配置完全指南：fake-ip 与 DNS 泄露 — https://tanqingbo.cn/clash-dns-guide/index.html
- TikTok IP 检测：住宅 / 原生 IP 是否适合 — https://ipok.io/t/tiktok-ip
- bgp.tools：查机房 ASN 的上游与 peering — https://bgp.tools
- Linode 各机房测速 / test IP — https://www.linode.com/speed-test/
- Hysteria2 端口跳跃（Port Hopping）文档 — https://v2.hysteria.network/docs/advanced/Port-Hopping/
- Hysteria2 Brutal 拥塞控制与带宽设置 — https://v2.hysteria.network/docs/advanced/Full-Server-Config/#bandwidth
- Xray XHTTP 传输（原 SplitHTTP）设计说明 — https://github.com/XTLS/Xray-core/discussions/4113
- Cloudflare 优选 IP 的原理与工具（CloudflareSpeedTest） — https://github.com/XIU2/CloudflareSpeedTest
- ClawCloud 新加坡 VPS — https://claw.cloud
- 我自己的 Hysteria2 低流量测试记录（2026-08-30），Table 01 的来源
