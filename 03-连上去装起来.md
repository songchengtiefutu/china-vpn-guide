# 科学上网第三课：连上去，装起来

> SSH 怎么连、密钥怎么弄、防火墙有几层。协议怎么选，VLESS、Reality、XHTTP、Hysteria2 每个词是什么意思。3x-ui 面板上三种入站每个格子填什么。懒得点的用勇哥脚本，更懒的丢给 AI。每段命令都有一键复制。
>
> 网页版：https://blog.songchengtie.me/posts/kexue-shangwang-03/ · 给 AI 的执行版：[AGENT.md](./AGENT.md)


机器到手了，这一课全是动手。先给结论，你只需要回答两个问题，家里是哪家运营商，机器的 IP 干不干净，对着这张表就能选出协议：

| 你的情况 | 用什么 | 为什么 |
| --- | --- | --- |
| 默认情况：IP 干净，回程不绕路 | VLESS + Reality（TCP 443） | 不要域名，不要证书，装完就能用，目前抗识别最好 |
| IP 脏（被 ping 墙、被标记过）、联通绕美、或者不想暴露 VPS 的 IP | VLESS + XHTTP + TLS，套 Cloudflare | 客户端只连 Cloudflare，VPS 藏在后面 |
| 电信到日本、联通到美西大水管，且 UDP 放得开 | Hysteria2，Brutal 填带宽 | QUIC 抗丢包，晚高峰硬顶 |
| 移动 | Reality 主用，Hysteria2 高位端口备用 | 移动对 UDP 有 QoS，443 经常静默丢包 |

装服务端有三条路，按你懒的程度选。想弄明白每个格子是什么的，自己装 3x-ui 面板点一遍（03 节）。不想点的，用勇哥的一键脚本，一路回车（04 节）。连回车都不想按的，把文章开头那个框里的话复制给任何一个 AI，它会问你几个问题然后替你装完（05 节）。三条路装出来的东西是一样的，区别只在你自己动多少手。所有命令右上角都有复制按钮，点它，别手动选，手动选经常多选一个空格，粘进去就报错。命令里的 `1.2.3.4` 是占位符，代表你自己 VPS 的 IP，复制完先把它换掉再回车，原样敲进去是连不上任何东西的。

<figure>
  <img src="https://blog.songchengtie.me/images/roads.webp" alt="三条路：一条直线，一条穿过云，一条是一串散点" width="900" height="900" loading="lazy">
  <figcaption><span>Fig. 01 · 三种走法：Reality 走直线，XHTTP 穿过 CDN 那朵云，Hysteria2 是一把散出去的 UDP 包</span><span>Plate Nº 07</span></figcaption>
</figure>

## 01 SSH 怎么连上去？

买完机器，商家会给你三样东西：IP、用户名、密码（或者一把密钥）。连上去用 SSH，Windows 10 之后自带，打开终端（Windows Terminal 或者 PowerShell）敲一行：

```powershell
ssh root@1.2.3.4    # 1.2.3.4 换成你的 IP，井号和后面这段注释不用管
```

把 `1.2.3.4` 换成你的 IP，后面所有命令里的 1.2.3.4 都一样。第一次连会问你 `Are you sure you want to continue connecting (yes/no)?`，敲 `yes` 回车，这是在记住这台机器的指纹。然后输密码，输的时候屏幕上什么都不显示，这是正常的，不是键盘坏了，输完回车。看到 `root@xxx:~#` 就进去了。macOS 和 Linux 一样，打开终端敲同一行。

商家改过 SSH 端口的（搬瓦工默认就不是 22），加 `-p`：

```bash
ssh -p 29999 root@1.2.3.4    # 29999 换成商家给的端口，1.2.3.4 换成你的 IP
```

谷歌云、Azure、甲骨文这三家不给你 root 密码，用的是密钥加一个普通用户。谷歌云在控制台里有个浏览器 SSH 按钮，点开就是终端。甲骨文创建机器时让你下载一把私钥，用户名是 `ubuntu` 或者 `opc`，连的时候带上：

```bash
ssh -i ~/Downloads/ssh-key.key ubuntu@1.2.3.4    # 路径换成你下载的私钥，IP 换成你的
```

进去之后先敲 `sudo -i` 切成 root，后面所有命令都按 root 来写，省得每行加 sudo。

密码登录能用，但建议第一天就换成密钥，全世界的机器人 24 小时在扫 22 端口试密码。本机生成一对：

```powershell
ssh-keygen -t ed25519
```

一路回车。公钥在 `~/.ssh/id_ed25519.pub`，把它塞到服务器上。macOS 和 Linux 一行：

```bash
ssh-copy-id root@1.2.3.4    # IP 换成你的
```

Windows 没有 `ssh-copy-id`，用这行：

```powershell
# 1.2.3.4 换成你的 IP
type $env:USERPROFILE\.ssh\id_ed25519.pub | ssh root@1.2.3.4 "mkdir -p ~/.ssh && cat >> ~/.ssh/authorized_keys"
```

之后 `ssh root@1.2.3.4` 就不问密码了。确认能免密进去之后，再把密码登录关掉：`nano /etc/ssh/sshd_config`，找到 `PasswordAuthentication`，改成 `no`，去掉前面的 `#`，Ctrl+O 保存，Ctrl+X 退出，然后 `systemctl restart ssh`。没确认免密能进就关密码，等于把自己锁在门外，机器只能重装。

接下来这几个命令，整篇文章会反复用到：

```bash
apt update && apt upgrade -y      # 更新系统，新机器第一件事
systemctl status xray             # 看某个服务在不在跑（xray 换成任何服务名）
systemctl restart xray            # 重启它
journalctl -u xray -n 50          # 看它最近 50 行日志，报错都在这
nano /etc/xxx.conf                # 编辑文件，Ctrl+O 保存，Ctrl+X 退出
ss -tulnp                         # 看哪些端口在监听
curl -4 ip.sb                     # 这台机器的公网 IP
exit                              # 退出
```

还有一种命令你会在所有教程里看到：

```bash
bash <(curl -Ls https://某个地址/install.sh)
```

意思是从那个地址下载一个脚本，直接交给 bash 执行。方便，但等于把整台机器交给写脚本的人，所以只跑你信得过的仓库的脚本，地址要从项目的 GitHub 页面复制，别从搜索结果里的第三方站复制。

最后是防火墙，新手卡得最多的地方。防火墙有两层。一层在商家后台，谷歌云叫 VPC 防火墙规则，甲骨文叫安全列表，Azure 叫网络安全组，默认只开 22，你在机器里装什么都连不上，要去后台把你用的端口放行。另一层在系统里，Ubuntu 用 ufw：

```bash
ufw allow 22
ufw allow 443
ufw enable
```

甲骨文的 Ubuntu 镜像还多一层，系统里预置了一套祖传 iptables 规则，把除了 22 以外的全拒了，ufw 管不到它。要放行 443 得这样：

```bash
iptables -I INPUT -p tcp --dport 443 -j ACCEPT
iptables -I INPUT -p udp --dport 443 -j ACCEPT
netfilter-persistent save
```

装好东西连不上，先查这三层，十有八九是其中一层没开。

## 02 协议怎么选？每个词是什么意思？

3x-ui 面板上一个入站要填十几个格子，不知道每个词是什么意思的话，填起来像抽奖。所以先把概念拆开。

一个节点是三层叠起来的。最里面是协议，规定怎么认人、怎么把数据包起来，这里用 VLESS，它自己不加密，只是带一个 UUID 当身份证。中间是传输，规定数据以什么形式在网上跑，TCP 是裸跑，XHTTP 是拆成一串 HTTP 请求跑。最外面是安全，TLS 或者 Reality，负责加密和伪装。Reality 只能配 TCP，XHTTP 要过 CDN 就只能配 TLS。Hysteria2 是另一套东西，它自己就是完整的一层，跑在 UDP 上，不和上面三层搭配。

<figure>
  <img src="https://blog.songchengtie.me/images/mask.webp" alt="一座宏伟建筑的正面，后面躲着一个小黑方块" width="900" height="900" loading="lazy">
  <figcaption><span>Fig. 02 · Reality：来敲门的人看到的是一座大建筑的正面，你的机器躲在后面</span><span>Plate Nº 08</span></figcaption>
</figure>

Reality 的思路是借别人的证书。你在服务端填一个目标网站，比如 `www.microsoft.com:443`，客户端握手时 SNI 就写这个域名。GFW 或者任何人来探测，服务端把握手原样转给微软，探测者拿到的是微软真实的证书，看起来这个 IP 上就是微软的一台机器。只有带着正确 x25519 密钥的客户端，服务端才会切换成代理。所以它不需要域名，不需要证书，也没有 TLS in TLS 那种一层套一层的特征。目标网站要挑一个支持 TLS 1.3 和 HTTP/2、不做跳转的，Xray 文档建议挑一个和你的 VPS 在同一家机房、同一个 ASN 的站，这样连 IP 段都对得上，`www.microsoft.com`、`www.apple.com` 这类是通用的兜底选择。

XHTTP 的思路是变成一个普通网站。它把上行拆成一个个 POST 请求，下行用一个长连接的 GET 流式返回，在 Cloudflare 眼里就是一个网站在被人访问，所以能过 CDN。过了 CDN 就有两个好处：客户端连的是 Cloudflare 的 IP，你的 VPS 被墙了也不影响；Cloudflare 的 IP 段里能挑出落在香港的，联通绕美的问题上一课讲过，走这条路能从 1800 ms 压到 400。代价是必须有域名和证书，多一层 HTTP，单次请求比直连慢一点，Cloudflare 免费版还有公平使用限制。我自己现在的主力线路就是这一条。

Hysteria2 跑在 QUIC 上，也就是 UDP。它的拥塞控制有两种，默认 BBR，填了带宽就变成 Brutal，按你填的速率固定往外发，丢多少补多少，晚高峰丢包 5% 到 10% 的线路上它几乎无感。电信到日本、联通到美西大水管，这两种情况 Hysteria2 收益最大。两个前提。一是 UDP 得放得开，移动那种跑一会儿就限速的、公司和校园网直接掐 UDP 的，它比 TCP 还差。二是带宽照实填，写你实际带宽的八九成。填大了它就真按那个速率往外喷，丢的包全靠重发补，流量翻倍，先是商家找你谈话，然后 IP 进黑名单。速度再快，开到 IP 被黑掉就什么都没了。

再就是 sing-box。它是另一个内核，协议支持得更全，勇哥的脚本默认用它。我自己没怎么用过，这篇的手动部分全按 Xray 写，3x-ui 底下也是 Xray。你用勇哥的 sing-box 脚本装出来的节点，客户端一样能连，概念都是一样的。

## 03 3x-ui 怎么装？三种入站怎么建？

3x-ui 是一个网页面板，底下跑的是 Xray 内核，你在网页上填格子，它帮你生成配置、生成分享链接和订阅。装它一行：

```bash
bash <(curl -Ls https://raw.githubusercontent.com/mhsanaei/3x-ui/master/install.sh)
```

装完最后会打印面板地址、用户名、密码，端口和路径都是随机生成的，把这几行复制下来存好。之后随时敲 `x-ui` 能进管理菜单，忘了密码在里面重置。把面板端口在 ufw 和商家后台放行，然后浏览器打开 `http://你的IP:端口/路径/`，就进面板了。进去是这样的：

<figure>
  <img src="https://blog.songchengtie.me/images/3xui-status.webp" alt="3x-ui 系统状态页" width="1400" height="776" loading="lazy">
  <figcaption><span>Fig. 03 · 我自己那台的系统状态页：1 核 1 G 的 Linode，跑了 21 天，内存用了四成，Xray 版本在右上角</span><span>Plate Nº 09</span></figcaption>
</figure>

左边菜单里你会常用的只有三个：入站（建节点）、客户端（给每个设备发一个 UUID，能看到谁在线、用了多少流量）、面板设置（订阅、面板端口）。

面板本身是明文 HTTP，密码是在公网上裸奔的。不想折腾证书的话，最省事的做法是不把面板端口对公网开，用 SSH 隧道连：本机敲 `ssh -L 54321:127.0.0.1:54321 root@1.2.3.4`（54321 换成你的面板端口），然后浏览器打开 `http://127.0.0.1:54321/路径/`。隧道只要 22 端口，面板端口在防火墙上不用开。

进面板之后先做两件事。左边菜单的面板设置里，把订阅打开，之后每个节点会自动有订阅链接。再回到 SSH，开 BBR，3x-ui 的 `x-ui` 菜单里有这一项，或者手动：

```bash
cat >> /etc/sysctl.conf <<'EOF'
net.core.default_qdisc=fq
net.ipv4.tcp_congestion_control=bbr
EOF
sysctl -p
sysctl net.ipv4.tcp_congestion_control   # 打印 bbr 就对了
```

然后建入站。点入站列表，添加入站，下面三种各建一个，用不上的可以跳过。建完的样子：

<figure>
  <img src="https://blog.songchengtie.me/images/3xui-inbounds.webp" alt="3x-ui 入站列表" width="1400" height="776" loading="lazy">
  <figcaption><span>Fig. 04 · 我的入站列表：443、8443、2053 三个 Reality，一个 XHTTP，两个老的 VMess WS 留着给旧设备。协议那一列就是 02 节说的三层：VLESS / TCP / Reality</span><span>Plate Nº 10</span></figcaption>
</figure>

一个入站可以挂多个客户端，家里每台设备一个，谁用了多少流量一目了然，出问题也知道是哪台设备。

### Reality 入站

1. 协议选 VLESS，端口 443。
2. 客户端那一栏，ID 就是 UUID，面板会随机给你一个，不用改。Flow 选 `xtls-rprx-vision`。
3. 传输方式 TCP。安全选 Reality。
4. 目标（Dest / Target）填 `www.microsoft.com:443`，SNI（服务器名称）填 `www.microsoft.com`。想更稳，换成和 VPS 同机房的站。
5. uTLS 指纹选 chrome。
6. 私钥公钥点那个获取新证书（Get New Cert）按钮，它会跑 `xray x25519` 生成一对。Short ID 也点生成。
7. 保存。

原理上，第 4 步填的站就是探测者会看到的那个网站，第 6 步生成的密钥就是只有你的客户端才有的那把钥匙。分享链接里带的是公钥，新版 Xray 把它叫 `pbk`，在客户端配置里叫 `password`，同一个东西。

### XHTTP 入站，套 Cloudflare

Cloudflare 那边先弄好三件事。DNS 里加一条 A 记录指向 VPS 的 IP，代理状态开着（橙色云朵）。SSL/TLS 的加密模式选完全（严格）。SSL/TLS 下面的源服务器那一页，创建证书，Cloudflare 会给你一张 15 年的源站证书和私钥，两段文本都复制下来存好，这张证书只有 Cloudflare 认，正好够用。

然后回面板建入站：

1. 协议 VLESS，端口 2053。Cloudflare 只代理这几个 HTTPS 端口：443、2053、2083、2087、2096、8443，443 已经给 Reality 了，所以用 2053。
2. Flow 留空，XHTTP 不用 vision。
3. 传输方式选 XHTTP。路径（Path）填一串随机字符，比如 `/a9f3k2`，Host 填你的域名，模式（Mode）选 packet-up，兼容性最好，Cloudflare 什么都不用开。
4. 安全选 TLS，证书那里选粘贴内容，把 Cloudflare 给的证书和私钥分别贴进去。
5. 保存。ufw 和商家后台放行 2053。

客户端这边，服务器地址就填你的域名，端口 2053，SNI 和 Host 也填域名，路径和模式跟服务端一致。这样走的是 Cloudflare 官方分配的节点，哪个近走哪个，省心。上一课说的优选 IP 是进阶玩法：用 CloudflareSpeedTest 跑一遍，挑延迟最低、落在香港的那批填到服务器地址里，域名不变。它确实能把联通的延迟压下来一大截，但优选出来的 IP 隔一两周就得重跑，因为 Cloudflare 的路由会变。懒得弄的直接用域名，效果差一点，但不用维护。

### Hysteria2

3x-ui 新版能直接建 Hysteria2 入站，Xray 内核是 2026 年 3 月才加的这个功能。我更习惯用 Hysteria 官方的脚本单独装，配置文件就十几行，出了问题好查：

```bash
bash <(curl -fsSL https://get.hy2.sh/)
```

没有域名就自签一张证书，客户端那边跳过校验：

```bash
openssl req -x509 -nodes -newkey ec:<(openssl ecparam -name prime256v1) \
  -keyout /etc/hysteria/key.pem -out /etc/hysteria/cert.pem \
  -subj "/CN=www.bing.com" -days 3650
chown hysteria /etc/hysteria/*.pem
```

写配置 `nano /etc/hysteria/config.yaml`：

```yaml
listen: :20000-50000        # 移动用户填端口段，就是端口跳跃；电信联通可以直接 :443
tls:
  cert: /etc/hysteria/cert.pem
  key: /etc/hysteria/key.pem
auth:
  type: password
  password: 换成一串长密码
bandwidth:
  up: 100 mbps              # VPS 实际上行的八九成
  down: 300 mbps            # 你家宽带下行的八九成
masquerade:
  type: proxy
  proxy:
    url: https://www.bing.com/
    rewriteHost: true
```

`listen` 填端口段，Hysteria 自己会用 iptables 把整段 UDP 端口收到一起，客户端每隔一会儿换一个端口，移动的 QoS 认端口不认流。`bandwidth` 填了就是 Brutal，不填就是 BBR，联通美西的那种大水管一定要填。`masquerade` 是有人用浏览器打开你这个端口时看到的东西，这里给它一个 bing。

```bash
systemctl enable --now hysteria-server
systemctl status hysteria-server
ufw allow 20000:50000/udp
```

商家后台同样要放行这段 UDP。客户端配置：服务器 `1.2.3.4:20000-50000`，密码，SNI 填 `www.bing.com`，跳过证书校验（insecure），带宽填和服务端一样的数。

建完之后跑一小时看会不会掉速，掉了就是运营商在掐 UDP，主用换回 Reality。

## 04 懒得点面板：勇哥一键脚本怎么用？

不想在面板上填格子，用勇哥（yonggekkk）的脚本。两个可以选。

sing-box 版，内核是 sing-box，一条命令装出 VLESS Reality、VMess WebSocket 加 Argo、Hysteria2、TUIC、AnyTLS 五种，支持纯 IPv6 机器和 ARM：

```bash
bash <(wget -qO- https://raw.githubusercontent.com/yonggekkk/sing-box-yg/main/sb.sh)
```

x-ui 版，是一个改过的 x-ui 面板，带 Reality、XHTTP、Hysteria2 和 Argo 隧道：

```bash
bash <(wget -qO- https://raw.githubusercontent.com/yonggekkk/x-ui-yg/main/install.sh)
```

跑起来是一个数字菜单，选 1 安装，接下来每一步它都会问你，端口、UUID、Reality 目标站这些不懂就直接回车用默认值。装完它把所有节点的分享链接和聚合订阅一次打印出来，复制到客户端就完了。之后再敲同一条命令能回到菜单，改端口、换协议、看链接都在里面。

两点要知道。一是 x-ui-yg 的 README 自己写了，二进制文件是闭源的，介意的用 3x-ui 或者 sing-box 版。二是它装出来的东西你不一定知道每一项是什么，出了问题不好查。所以我的建议是，用脚本之前把 02 节读一遍，至少知道菜单里问你的那些词是什么。

Argo 隧道多说一句。它是 Cloudflare 的一个工具，在你的机器上开一条出站的隧道到 Cloudflare，效果和 03 节的 XHTTP 套 CDN 类似，机器不需要公网端口，被墙的 IP、甚至 NAT 后面的机器都能用。勇哥的脚本里它是选装的，IP 干净的话不用开。

## 05 连脚本都懒得跑：让 AI 装？

从挑机器、问你运营商、选协议，到装 Xray、写配置、开防火墙、跑验证，最后把分享链接和 Clash 片段打出来，我写成了一份给 AI 的执行版，就是文章开头那个框。它不讲原因，只有决策树、命令和判定标准，每一步做完都有验证，验证不过不进下一步。四课的内容它一份就够，AI 看这一份，人看这四篇。

用法分两种。手边有能跑命令的 AI（Claude Code、Codex、Cursor、OpenCode，随便哪个），在本机终端里启动，把框里那句话粘进去，它会先读执行版，然后问你几件事：运营商、有没有机器、有没有域名、登录方式、要不要 Hysteria2。你回答完它就开始通过 SSH 干活，每一条命令你都看得到，不放心随时打断。只有网页版的 ChatGPT 或者 Claude 也行，它跑不了命令，但会一步一步告诉你敲什么、把输出贴回来给它看，慢一点，一样能装完。

还没有梯子的人也能用这一套。执行版里写了让 AI 先问你国内能打开哪些网站，打不开 GitHub 就换国内能开的商家、走支付宝，客户端安装包让 AI 从 VPS 上下好再传给你。

几个注意。它需要能免密 SSH 到你的 VPS，所以 01 节那一步密钥登录先自己做好，别把 root 密码贴进聊天。它默认装的是 Xray 官方内核加手写配置，没有面板，因为面板是给人点的，AI 用不上；你要面板的话跟它说一声，它会改装 3x-ui 然后把面板地址给你，入站你自己照 03 节建。它装完的东西和 03 节手动装的完全一样，之后你想改，看 03 节就知道东西在哪。

## 06 下一课

服务端装好了，下一课讲客户端：各平台用什么、订阅怎么导、DNS 分流怎么配才不会卡住、常见的坑一览。

## 参考

- 3x-ui 项目主页与安装脚本 — https://github.com/MHSanaei/3x-ui
- 勇哥 sing-box 一键脚本 — https://github.com/yonggekkk/sing-box-yg
- 勇哥 x-ui-yg 面板 — https://github.com/yonggekkk/x-ui-yg
- Xray REALITY 配置文档（target、serverNames、shortIds、password） — https://xtls.github.io/config/transports/reality.html
- XHTTP: Beyond REALITY（设计说明与 mode、xmux 参数） — https://github.com/XTLS/Xray-core/discussions/4113
- xray x25519 输出改为 PrivateKey / Password 的说明 — https://github.com/XTLS/Xray-core/discussions/5219
- Xray-core v26.3.27 加入 Hysteria2 入站 — https://github.com/MHSanaei/3x-ui/issues/3976
- Hysteria2 官方安装脚本与服务端完整配置 — https://v2.hysteria.network/docs/advanced/Full-Server-Config/
- Hysteria2 端口跳跃 — https://v2.hysteria.network/docs/advanced/Port-Hopping/
- Cloudflare 优选 IP 工具 CloudflareSpeedTest — https://github.com/XIU2/CloudflareSpeedTest
- 上一课：机器从哪来，钱怎么花 — ./02-机器和钱.md
